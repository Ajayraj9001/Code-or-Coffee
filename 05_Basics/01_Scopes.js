// Scops:- Block scope, Global scope, Function scope
let name = "Ajay"    
const id = 123
var b = 100

if(true) {
    console.log(b);    
}

console.log(name);
console.log(id);

// Note:- Try not to us 'var' classical variable to store data in JS, instead use let and const 

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
}

// Note:- In JavaScript everything is inside curly braces is called scope.
